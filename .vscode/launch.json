{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb-mi",
            "request": "launch",
            "lldbmipath": "lldb",
            "target": "./build/ds",
            "cwd": "${workspaceRoot}"
        }
    ]
}
