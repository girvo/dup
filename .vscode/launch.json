{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug docker_socket",
            "type": "gdb",
            "request": "launch",
            "target": "./build/ds",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "ds"
        }
    ]
}
